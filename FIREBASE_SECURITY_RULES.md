# Firebase Security Rules

Эти правила нужно скопировать в Firebase Console для настройки безопасности.

## 📝 Firestore Security Rules

**Где настроить:** Firebase Console → Firestore Database → Rules

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Правила для коллекции packages внутри каждого пользователя
    match /users/{userId}/packages/{noteId} {
      // Разрешаем чтение и запись только если:
      // 1. Пользователь аутентифицирован (request.auth != null)
      // 2. UID пользователя совпадает с userId в пути (request.auth.uid == userId)
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

---

## 📸 Firebase Storage Security Rules

**Где настроить:** Firebase Console → Storage → Rules

```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Правила для файлов в папке users/{userId}/packages/...
    match /users/{userId}/{allPaths=**} {
      // Разрешаем чтение и запись только если:
      // 1. Пользователь аутентифицирован (request.auth != null)
      // 2. UID пользователя совпадает с userId в пути (request.auth.uid == userId)
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

---

## 🔒 Что это дает:

1. **Изоляция данных:** Каждый пользователь видит только свои данные
2. **Защита от несанкционированного доступа:** Невозможно прочитать или изменить чужие данные
3. **Автоматическая проверка:** Firebase проверяет права при каждом запросе
4. **Без бэкенда:** Не нужен собственный сервер для проверки прав

---

## 📋 Инструкция по применению:

### 1. Настройка Firestore Rules:
1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите проект **mycorc**
3. Перейдите в **Firestore Database** (левое меню)
4. Нажмите на вкладку **Rules** (вверху)
5. Скопируйте правила из секции "Firestore Security Rules" выше
6. Нажмите **Publish** (опубликовать)

### 2. Настройка Storage Rules:
1. В Firebase Console перейдите в **Storage** (левое меню)
2. Нажмите на вкладку **Rules** (вверху)
3. Скопируйте правила из секции "Firebase Storage Security Rules" выше
4. Нажмите **Publish** (опубликовать)

---

## ⚠️ Важно:

- Эти правила применяются **немедленно** после публикации
- Без этих правил данные будут **открыты для всех** (небезопасно!)
- Правила работают только для **аутентифицированных пользователей**
- Если пользователь не залогинен (`request.auth == null`), доступ **запрещен**

---

## 🧪 Тестирование правил:

После применения правил можно протестировать их прямо в Firebase Console:

1. В Rules editor нажмите на кнопку **Rules Playground** (справа вверху)
2. Выберите **Simulate read** или **Simulate write**
3. Укажите путь: `users/test-user-123/packages/test-note-456`
4. Добавьте auth UID: `test-user-123`
5. Нажмите **Run** - должно быть ✅ **Allow**

Если попробовать с другим UID (например, `another-user`), будет ❌ **Deny** - это правильно!

---

## 📚 Дополнительная информация:

- [Firestore Security Rules Documentation](https://firebase.google.com/docs/firestore/security/get-started)
- [Storage Security Rules Documentation](https://firebase.google.com/docs/storage/security)
